import styled from "styled-components";

const StyledSection = styled.section`
  height: 20vh;
  width: 60wh;
  color: #fff;
  margin: 0 auto 0 auto;
`;

const TimerBox = () => {
  return <StyledSection>Start</StyledSection>;
};

export default TimerBox;
