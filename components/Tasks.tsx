import styled from "styled-components";

const StyledSection = styled.section`
  width: 60vw;
  height: 20vh;
  background: orange;
  color: #fff;
`;

const Tasks = () => {
  return <StyledSection>Tasks</StyledSection>;
};

export default Tasks;
